int singleElement(int arr[] ,int N) {
        // code here
        sort(arr,arr+N);
        for(int i=0;i<N;i=i+3){
            if(i+1<N && arr[i]!=arr[i+1] ){
                return arr[i];
            }
        }
        return arr[N-1];
    }
